@model PlantShop.Models.Video
@{
    ViewData["Title"] = "Thêm video mới";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<main class="app-content">
    <div class="app-title">
        <ul class="app-breadcrumb breadcrumb">
            <li class="breadcrumb-item">Quản lý video</li>
            <li class="breadcrumb-item"><a href="#">Thêm video</a></li>
        </ul>
    </div>

    <div class="row">
        <div class="col-md-12">
            <div class="tile">
                <h3 class="tile-title">Thêm video hướng dẫn mới</h3>
                <div class="tile-body">
                    <div asp-validation-summary="All" class="alert alert-danger"></div>

                    <form asp-action="Create" method="post" class="row">
                        <div class="form-group col-md-8">
                            <label asp-for="Title" class="control-label">Tiêu đề <span class="text-danger">*</span></label>
                            <input asp-for="Title" class="form-control" required />
                            <span asp-validation-for="Title" class="text-danger"></span>
                        </div>

                        <div class="form-group col-md-4">
                            <label asp-for="YoutubeId" class="control-label">ID YouTube <span class="text-danger">*</span></label>
                            <input asp-for="YoutubeId" class="form-control" required placeholder="vd: dQw4w9WgXcQ" />
                            <span asp-validation-for="YoutubeId" class="text-danger"></span>
                            <small class="text-muted">Lấy từ link YouTube: https://youtu.be/<strong>ID_VIDEO</strong></small>
                        </div>

                        <div class="form-group col-md-12">
                            <label asp-for="Description" class="control-label">Mô tả</label>
                            <textarea asp-for="Description" class="form-control" rows="3"></textarea>
                            <span asp-validation-for="Description" class="text-danger"></span>
                        </div>

                        <div class="form-group col-md-3">
                            <label asp-for="Duration" class="control-label">Thời lượng (giây)</label>
                            <input asp-for="Duration" class="form-control" type="number" placeholder="vd: 360 = 6 phút" />
                            <span asp-validation-for="Duration" class="text-danger"></span>
                        </div>

                        <div class="form-group col-md-3">
                            <label asp-for="Category" class="control-label">Danh mục</label>
                            <input asp-for="Category" class="form-control" placeholder="vd: Cây phong thủy" />
                            <span asp-validation-for="Category" class="text-danger"></span>
                        </div>

                        <div class="form-group col-md-3">
                            <label asp-for="SortOrder" class="control-label">Thứ tự hiển thị</label>
                            <input asp-for="SortOrder" class="form-control" type="number" value="0" />
                            <span asp-validation-for="SortOrder" class="text-danger"></span>
                            <small class="text-muted">Số càng nhỏ càng hiển thị đầu</small>
                        </div>

                        <div class="form-group col-md-3">
                            <label asp-for="IsActive" class="control-label">Trạng thái</label>
                            <select asp-for="IsActive" class="form-control">
                                <option value="true">Hiển thị</option>
                                <option value="false">Ẩn</option>
                            </select>
                        </div>

                        <div class="form-group col-md-12">
                            <label>Preview video</label>
                            <div class="embed-responsive embed-responsive-16by9" id="videoPreview" style="display: none;">
                                <iframe class="embed-responsive-item" id="youtubeFrame" src="" frameborder="0" allowfullscreen></iframe>
                            </div>
                            <div id="noVideoPreview" class="alert alert-info">
                                Nhập ID YouTube để xem trước video
                            </div>
                        </div>

                        <div class="form-group col-md-12">
                            <button class="btn btn-save" type="submit"><i class="fa fa-save"></i> Lưu lại</button>
                            <a asp-action="Index" class="btn btn-cancel"><i class="fa fa-times"></i> Hủy bỏ</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</main>

@section Scripts {
    <script>
        // Preview video khi nhập YoutubeId
        $('#YoutubeId').on('input', function() {
            var youtubeId = $(this).val();
            if (youtubeId) {
                var embedUrl = 'https://www.youtube.com/embed/' + youtubeId;
                $('#youtubeFrame').attr('src', embedUrl);
                $('#videoPreview').show();
                $('#noVideoPreview').hide();
            } else {
                $('#videoPreview').hide();
                $('#noVideoPreview').show();
            }
        });
    </script>
}